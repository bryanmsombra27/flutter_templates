import 'package:flutter/material.dart';

class Call extends StatelessWidget {
  const Call({super.key});

  static const name = "call";

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("CALL"));
  }
}
